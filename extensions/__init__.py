"""
Extension-point stubs for modules planned beyond the 50% MVP.

These files document interfaces so the research system can grow without
rewriting the current Streamlit → profile → prompt → SD → sketch pipeline.
"""
